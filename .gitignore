# Deny-all by default, then explicitly allow tracked dotfiles.
/*

# Root-level dotfiles
!/.gitconfig
!/.gitignore
!/.gitignore_global
!/.tigrc
!/.zshrc

# Only keep Neovim config under .config
!/.config/
/.config/*
!/.config/nvim/
